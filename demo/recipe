# Which recording recipe demo/record.sh uses. One of the names in demo/lib/.
#
# playwright — this piece. The deliverable is a filed tree and an index sheet
#              the client opens, so the clip opens it: demo/scene.py renders
#              filed/index.xlsx in a grid, read off disk after the run that
#              wrote it. See demo/scene.py.
#
# vhs        — the terminal sibling, still here and still working. Try it with
#              DEMO_RECIPE=vhs DEMO_OUT_DIR=demo/out-terminal ./demo/record.sh
#              and see demo/demo.tape.
playwright
