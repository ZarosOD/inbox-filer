.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
/build/

# Run artefacts: the filing cabinet the tool writes.
/filed/

# Fetched toolchains and demo scratch. demo/out/ is committed on purpose:
# the clip is part of the deliverable. The leading slash on /filed/ above
# matters — a bare pattern would swallow a fixture of the same name.
demo/.toolchain/
demo/.scratch/
demo/out/raw/
